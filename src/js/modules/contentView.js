
// preview.spawn.contentView

{
	init() {
		
	},
	async dispatch(event) {
		let APP = preview,
			Self = APP.spawn.contentView,
			top;
		switch (event.type) {
			// native events
			case "scroll":
				break;
			// custom events
			case "scroll-to-page":
				break;
		}
	}
}
