
let Test = {
	init(APP, Spawn) {
		// setTimeout(() => APP.dispatch({ type: "save-file-as", spawn }), 300);

		// temp
		// setTimeout(() => Self.dispatch({ type: "tab.new", spawn: Spawn }), 300);
		// setTimeout(() => Spawn.find(`.toolbar-tool_[data-click="toggle-sidebar-view"]`).trigger("click"), 1200);
		// setTimeout(() => {
		// 	if (Spawn.find("layout.show-blank-view").length) return;
		// 	Self.dispatch({ ...event, type: "merge-all-windows" });
		// }, 800);
	}
};
